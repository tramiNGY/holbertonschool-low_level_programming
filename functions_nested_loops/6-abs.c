#include <stdio.h>
#include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @int: integer
 * Return: returns int
*/
int _abs(int)
{
int a;

if (a >= 0)
return (a);
if (a < 0)
return (-a);
return (0);
}

